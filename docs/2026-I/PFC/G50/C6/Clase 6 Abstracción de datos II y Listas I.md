
# Como vamos

1. Utilizando objetos podemos representar conjuntos de datos tales como los relacionales $\mathbb{Q}$ están compuestos por dos números $x$ numerador y $y$ es el denominador, $y > 0$, $x \in \mathbb{Z}$, esto nos permite trabajar estos tipos de datos a un alto nivel sin precuparnos por su representación
2. Podemos definir métodos utilizando currificación, los métodos tienen un parámetro, pero implicitamente tienen un segundo que es el this
3. El this o ligadura dinamica. referenciar a la instanciación del mismo objeto, este es el tructo que utiliza la programacion O.O para que una clase pueda ser instanciada
4. Scala a las funciones currificadas permite invocarlas en notación infija
```scala
   obj.metodo(x)
   obt metodo x
```


# Temas.

1. [Herencia y clases abstracta](Herencia%20y%20clases%20abstracta.md)
2. [Listas](Listas.md)