## Definiciones
Estos algoritmos explorar todas la posibilidades
## Ventajas
- Llegar a la solución óptima
- Son fáciles implementar
## Desventajas
- Son computacionalmente costosos
- A medida que crece la entrada se vuelven inviables por el tiempo que toma encontrar una solución

```java
//Adivinar una clave de 4 digitos
bool adivinarClave(String clave) {
	for(int i=0; i<=n; i++) {  // n
		for (int j = 0 ...)  // n*n
			for (int k = 0 ...) // n³
				for (int w = 0 ...) // n⁴
	}
}
```
En este caso para averiguar la contraseña de 4 caracteres requiero $O(n⁴)$ operaciones en promedio **es muy costoso**

